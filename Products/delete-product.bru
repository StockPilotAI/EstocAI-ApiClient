meta {
  name: delete product
  type: http
  seq: 10
}

delete {
  url: {{apiUrl}}/api/v1/products/{{productId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
