meta {
  name: delete company
  type: http
  seq: 5
}

delete {
  url: {{apiUrl}}/api/v1/companies/{{companyId}}
  body: none
  auth: none
}

headers {
  X-User-Role: {{userRole}}
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
