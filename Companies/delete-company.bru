meta {
  name: delete company
  type: http
  seq: 5
}

delete {
  url: {{apiUrl}}/api/v1/companies/{{empresaId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
