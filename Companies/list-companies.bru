meta {
  name: list companies
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/api/v1/companies
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
